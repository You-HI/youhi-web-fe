import { Layout } from 'antd';

const Footer = () => {
  return (
    <Layout.Footer style={{ textAlign: 'center', backgroundColor: 'gray' }}>
      Footer 영역
    </Layout.Footer>
  );
};

export default Footer;
